class Area:
    def __init__(self, pi, r, area):
        self.radius = r
        self.pai = pi
        self.area = area

    def circle(self, area):
        return self.area = self.pi * (self.radius * self)

print(Area.circle())
